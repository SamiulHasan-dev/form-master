/* 
    1. use form tag and onSubmit handler with event (e) From e access e.target[name pf the input field] and name to get the value.

    2. controlled element: use individual field state for each input field

    3. controlled element: one object in a state holding all the input fields value

    4. uncontrolled element: useRef to create a reference to the element and access value by using like: nameReg.current.value

    5. use hook to handle states and submit and error.example recheck hook from.

    5. Reuseable component
*/